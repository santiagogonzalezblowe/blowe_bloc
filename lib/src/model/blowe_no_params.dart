/// A class representing no parameters.
/// This can be used when an operation does not require any parameters.
class BloweNoParams {
  /// Creates an instance of BloweNoParams.
  const BloweNoParams();
}
